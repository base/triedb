[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
triedb = { path = ".." }
clap = { version = "4.4", features = ["derive"] }
hex = "0.4"
alloy-trie = { version = "0.7.6", features = ["arbitrary", "ethereum"] }

[[bin]]
name = "cli"
path = "src/main.rs"

